name: Diagnostic apply Yesenin Duncan companion validator pass one

on:
  push:
    branches: [research/yesenin-duncan-companion-editorial-pass1]
    paths:
      - 'scripts/diagnostic-apply-yesenin-duncan-companion-validator-pass1.py'
      - '.github/workflows/diagnostic-apply-yesenin-duncan-companion-validator-pass1.yml'
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: diagnostic-apply-yesenin-duncan-companion-validator-pass1
  cancel-in-progress: true

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: research/yesenin-duncan-companion-editorial-pass1
          fetch-depth: 0
      - name: Apply deterministic validator patch
        run: python scripts/diagnostic-apply-yesenin-duncan-companion-validator-pass1.py
      - name: Commit patched validator
        run: |
          if git diff --quiet; then
            echo 'No patch commit required.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add scripts/validate-yesenin-duncan-first-meeting-unpublished.ts
          git commit -m 'Strengthen Duncan companion editorial gate'
          git push origin HEAD:research/yesenin-duncan-companion-editorial-pass1
