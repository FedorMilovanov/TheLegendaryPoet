name: Temporary verified Gruzinov derived-text acquisition

on:
  pull_request:
    branches: [main]
    paths:
      - .github/workflows/acquire-gruzinov-derived-temporary.yml

permissions:
  contents: read

jobs:
  acquire:
    if: github.head_ref == 'editorial/longform-marathon-2026-08'
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Install PDF tools
        run: |
          sudo apt-get update
          sudo apt-get install -y poppler-utils

      - name: Download official Wikisource PDF export
        run: |
          set -euo pipefail
          mkdir -p acquisition
          curl \
            --fail \
            --location \
            --retry 6 \
            --retry-all-errors \
            --retry-delay 5 \
            --connect-timeout 30 \
            --max-time 900 \
            'https://ws-export.wmcloud.org/?format=pdf&lang=ru&page=%D0%95%D1%81%D0%B5%D0%BD%D0%B8%D0%BD_%D1%80%D0%B0%D0%B7%D0%B3%D0%BE%D0%B2%D0%B0%D1%80%D0%B8%D0%B2%D0%B0%D0%B5%D1%82_%D0%BE_%D0%BB%D0%B8%D1%82%D0%B5%D1%80%D0%B0%D1%82%D1%83%D1%80%D0%B5_%D0%B8_%D0%B8%D1%81%D0%BA%D1%83%D1%81%D1%81%D1%82%D0%B2%D0%B5_%28%D0%93%D1%80%D1%83%D0%B7%D0%B8%D0%BD%D0%BE%D0%B2%29' \
            --output acquisition/Gruzinov_Esenin_o_literature_i_iskusstve__Wikisource_DERIVED.pdf

      - name: Verify derived PDF bytes and first page
        run: |
          set -euo pipefail
          PDF='acquisition/Gruzinov_Esenin_o_literature_i_iskusstve__Wikisource_DERIVED.pdf'
          test "$(head -c 5 "$PDF")" = '%PDF-'
          file "$PDF" | tee acquisition/file.txt
          pdfinfo "$PDF" | tee acquisition/pdfinfo.txt
          PAGES="$(pdfinfo "$PDF" | awk -F: '/^Pages:/ {gsub(/ /, "", $2); print $2}')"
          test -n "$PAGES"
          test "$PAGES" -ge 10
          BYTES="$(stat -c%s "$PDF")"
          test "$BYTES" -gt 50000
          SHA="$(sha256sum "$PDF" | awk '{print $1}')"
          pdftoppm -f 1 -singlefile -png -r 160 "$PDF" acquisition/first-page
          test -s acquisition/first-page.png
          {
            echo 'source_kind=DERIVED_TEXT_PDF'
            echo 'source_platform=Russian Wikisource / Wikimedia export service'
            echo 'source_page=https://ru.wikisource.org/wiki/Есенин_разговаривает_о_литературе_и_искусстве_(Грузинов)'
            echo 'text_source_reported_by_wikisource=az.lib.ru'
            echo 'underlying_work_public_domain=true'
            echo 'facsimile_of_1927_edition=false'
            echo 'canonical_archival_master=false'
            echo "pdf_pages=$PAGES"
            echo "file_size_bytes=$BYTES"
            echo "sha256=$SHA"
            echo 'first_page_rendered=true'
            echo 'drive_upload_completed=false'
          } | tee acquisition/VERIFICATION.txt

      - name: Upload verified derived-text artifact
        uses: actions/upload-artifact@v4
        with:
          name: yesenin-source-gruzinov-wikisource-derived
          path: acquisition/
          if-no-files-found: error
          retention-days: 2
