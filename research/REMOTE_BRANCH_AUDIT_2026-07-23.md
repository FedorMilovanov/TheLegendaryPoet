# Аудит удалённых веток и план консолидации

Дата проверки: 2026-07-23

Статус: `ACTIVE / DO-NOT-BULK-MERGE`

Цель этого файла — прекратить параллельное дублирование одной и той же архитектуры разными агентами. Ни одна крупная ветка не должна сливаться целиком только потому, что в ней есть полезные файлы. Сначала извлекаются уникальные слои, затем выполняется проверка на актуальном `main`.

## Текущее состояние `main`

На момент аудита вершина `main` — commit `efed039b29fbb566554fe151a69ef102b0e401f7` (`Add files via upload`). Он добавил MP3 Есенина, две WebP-обложки и `public/audio/manifest.json`.

Исследовательская ветка PR #37 отстаёт от `main` ровно на этот аудиокоммит. Аудиофайлы в PR #37 вручную не копируются: их структура принадлежит аудиоветке и должна быть синхронизирована после решения расхождения путей изображений.

## Карта открытых PR

| PR | Ветка | Роль | Относительно текущего `main` | Решение |
|---:|---|---|---|---|
| #32 | `archive-collage-real-photos` | воспроизводимый коллаж из подлинных фото Маяковского и Бриков | diverged; `ahead 10 / behind 7`; уникальны workflow и два Python-скрипта | **Сохранить отдельно.** Не сливать до повторного аудита 30 карточек Commons, лицензий, SHA-256 и результата workflow. После проверки перенести только скрипты/manifest-подход либо закрыть как исследовательский эксперимент. |
| #34 | `work/yesenin-visual-series` | двухчастная биография Есенина | diverged; `ahead 7 / behind 5`; добавляет `yeseninPartOne.ts`, wrapper второй части и диагностический workflow | **Извлечь содержание, не merge целиком.** Часть I отсутствует в PR #37 и нужна. Перед переносом провести новый факт-чек, локализовать изображения, расширить источники до 40+, убрать прямые remote-image URL и встроить в текущий единый движок. |
| #37 | `work/local-images-playwright-wtoc` | главный исследовательский и архитектурный контур статей | diverged; `ahead 286 / behind 1`; 288 изменённых файлов | **Основная ветка консолидации.** Здесь продолжаются исследования Есенина/Маяковского, source policy, локальные архивные медиа, SEO, prerender и полный Playwright. До финальной вычитки остаётся draft. |
| #38 | `audit/playwright-total-article-check` | более ранний аудит/унификация article engine | diverged; `ahead 61 / behind 3`; сильное пересечение с #37 | **Заморозить как источник уникальных идей.** Сравнить `yeseninSources.ts`, `LongformPage`, audit tests и reduced-motion test с #37. Перенести только отсутствующее. После извлечения уникальных частей закрыть как superseded by #37. |
| #39 | `feat/community-ratings-audio` | рейтинги, Supabase, музыкальный архив и страницы релизов | diverged; `ahead 21 / behind 3` | **Чужая активная зона. Не изменять из #37.** Координация только через комментарии. После стабилизации путей MP3/WebP, manifest и валидатора ветка самостоятельно синхронизируется с `main`. |

## Аудио: установленная граница владения

Ветка PR #39 отвечает за:

- `FeaturedTrackPlayer` и страницы музыкальных релизов;
- `RatingsPage` и читательские рейтинги;
- Supabase/RPC-схему;
- `public/audio/manifest.json` и `validate-audio-assets`;
- маршруты `/ratings` и `/music/:slug`;
- структуру каталогов MP3 и обложек.

PR #37 не должен:

- переименовывать MP3;
- переносить WebP между `public/audio` и `public/images/music`;
- редактировать waveform/Media Session;
- менять права, SHA-256 или release metadata;
- копировать реализации PR #39 вручную.

Обнаруженное расхождение уже сообщено в PR #39: фактические WebP загружены в `public/audio`, тогда как ветка и поле `cover` ожидают `/images/music/...`.

## Уникальные слои, которые нужно извлечь

### Из PR #34

1. Каркас первой части биографии Есенина `1895–1921`.
2. Серийная навигация `part 1 / part 2`.
3. Разделы: Константиново, Спас-Клепики, Москва, Изряднова, Блок, Клюев, `Радуница`, санитарный поезд, революционная религиозная утопия, имажинизм.
4. Нравственная граница: литературный путь не отменяет ответственности перед Анной Изрядновой и сыном.
5. Запрет на перенос remote-image URL в production: сначала локализация, license ledger и media manifest.

### Из PR #38

1. Проверить, остались ли уникальными `tests/playwright-audit/*`.
2. Сверить `yeseninSources.ts` с source library PR #37.
3. Сверить reduced-motion сценарии с нынешним полным Chromium sweep.
4. Не переносить второй `LongformPage`, если текущий `ArticleRenderer` уже покрывает тот же контракт.
5. Не возвращать отдельный диагностический workflow, если проверка уже встроена в основной CI.

### Из PR #32

1. Список исходных Commons-файлов и stable page URLs.
2. Лицензии и авторство каждого объекта.
3. SHA-256 скачанных оригиналов.
4. Контроль запрета на генерацию лиц.
5. CSV/JSON manifest как потенциальный источник для общего архивного медиареестра.

## Порядок действий

1. Продолжать факт-чек и source audit в PR #37.
2. Перенести первую часть Есенина из #34 вручную, после полного факт-чека и локализации изображений.
3. Извлечь из #38 только уникальные проверки; затем закрыть #38 как superseded.
4. Не трогать #39 до сигнала аудиоагента о готовности путей и зелёном `validate:audio`.
5. Прогнать workflow #32 отдельно; по результату либо перенести воспроизводимый manifest, либо оставить PR архивным экспериментом.
6. После стабилизации аудио синхронизировать PR #37 с `main` обычным merge commit без force-push.
7. Не объединять #34, #37, #38 и #39 одним массовым merge: конфликты в `App.tsx`, `package.json`, sitemap, prerender, navigation и source data слишком велики.

## Условия закрытия веток

- #38 закрывается только после письменной таблицы «уникально / уже есть в #37 / отброшено».
- #34 закрывается только после появления проверенной части I Есенина в основной исследовательской ветке.
- #32 закрывается после получения и проверки артефакта либо после документированного отказа от подхода.
- #39 закрывается/сливается только владельцем аудиоработы после проверки реальных MP3/WebP и маршрутов.
- #37 становится ready for review только после редакционной вычитки всех публичных формулировок и зелёного CI на синхронизированном `main`.
