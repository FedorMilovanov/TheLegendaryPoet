// Keep the historical workflow entrypoint stable while the WebKit checks live
// in isolated fresh-context tests that avoid cumulative Linux WebKit scroll exits.
import './mobile-webkit-isolated.spec.mjs';
