# Dependency security refresh trigger

Temporary marker used to run the already-committed CI artifact generator on the next pull-request head. Remove after the generated lock and audit review are applied.
